# Dev/test dependencies (not needed to run the server).
# Property-based testing extension (AI-DLC: PBT = Partial — pure functions only).
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
hypothesis>=6.100.0
