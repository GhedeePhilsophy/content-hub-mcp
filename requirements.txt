# Ghedee Content Hub MCP server dependencies

# MCP server runtime
mcp>=1.2.0

# Social Calendar spreadsheet read/write
openpyxl>=3.1.0

# Text overlay on generated carousel slides
pillow>=10.0.0

# First-frame posters for video rows in the review preview (bundles ffmpeg)
imageio>=2.30.0
imageio-ffmpeg>=0.4.9

# Media generation (images: Nano Banana, video: Veo)
google-genai>=1.0.0

# Google Drive (upload, existence-check, download)
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
